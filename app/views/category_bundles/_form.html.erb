<%= form_for(@category_bundle) do |f| %>
  <% if @category_bundle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_bundle.errors.count, "error") %> prohibited this category_bundle from being saved:</h2>

      <ul>
      <% @category_bundle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cat_id %><br />
    <%= f.number_field :cat_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
